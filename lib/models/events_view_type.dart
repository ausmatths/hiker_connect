/// Enum representing different view types for events display
enum EventsViewType {
  /// List view displays events in a vertical scrolling list
  list,

  /// Grid view displays events in a card-based grid layout
  grid,

  /// Map view displays events on a geographical map
  map
}